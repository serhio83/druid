# druid

Parses requests from Prometheus`s alertmanager
kubernetes ready service

#### build:

```
make deps
make build
```

#### build docker container:

```
make container
```

#### run on local docker:

```
make run
```
